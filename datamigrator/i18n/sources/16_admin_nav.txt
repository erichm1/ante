# The navbar's Admin section (Reports, Incidents, Logs, Administration, Django admin).
# en || pt-BR || es || fr || de || it || ja || zh
Django admin || Admin Django || Admin de Django || Admin Django || Django-Admin || Admin di Django || Django 管理サイト || Django 管理后台
Admin — reports, incidents and logs, plus user administration for administrators. || Admin — relatórios, incidentes e logs, além da administração de usuários para administradores. || Admin: informes, incidentes y registros, además de la administración de usuarios para administradores. || Admin — rapports, incidents et journaux, plus l'administration des utilisateurs pour les administrateurs. || Admin — Berichte, Vorfälle und Protokolle sowie die Benutzerverwaltung für Administratoren. || Admin — report, incidenti e log, più l'amministrazione degli utenti per gli amministratori. || 管理 — レポート、インシデント、ログ。管理者はユーザー管理も利用できます。 || 管理 — 报表、事件和日志，管理员还可进行用户管理。
Administration — who can sign in, their departments and what each person may use. || Administração — quem pode entrar, seus departamentos e o que cada pessoa pode usar. || Administración: quién puede iniciar sesión, sus departamentos y qué puede usar cada persona. || Administration — qui peut se connecter, dans quel département et ce que chacun peut utiliser. || Verwaltung — wer sich anmelden darf, in welcher Abteilung und was jede Person nutzen darf. || Amministrazione — chi può accedere, i reparti e ciò che ogni persona può usare. || 管理 — サインインできるユーザー、所属部門、各ユーザーが使える機能を管理します。 || 管理 — 谁可以登录、所属部门以及每个人可以使用的功能。
