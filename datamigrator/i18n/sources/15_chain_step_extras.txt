# Headers and query parameters on the classic chain page (add / edit step, steps table).
# en || pt-BR || es || fr || de || it || ja || zh
+ Add parameter || + Adicionar parâmetro || + Añadir parámetro || + Ajouter un paramètre || + Parameter hinzufügen || + Aggiungi parametro || + パラメーターを追加 || + 添加参数
No extra headers. || Nenhum cabeçalho extra. || Sin cabeceras adicionales. || Aucun en-tête supplémentaire. || Keine zusätzlichen Header. || Nessun header aggiuntivo. || 追加のヘッダーはありません。 || 没有额外的请求头。
No query parameters. || Nenhum parâmetro de consulta. || Sin parámetros de consulta. || Aucun paramètre de requête. || Keine Abfrageparameter. || Nessun parametro di query. || クエリパラメーターはありません。 || 没有查询参数。
— extra headers sent with this request (they override the connection's headers of the same name). || — cabeçalhos extras enviados com esta requisição (substituem os cabeçalhos da conexão de mesmo nome). || — cabeceras adicionales enviadas con esta petición (sustituyen a las de la conexión con el mismo nombre). || — en-têtes supplémentaires envoyés avec cette requête (ils remplacent ceux de la connexion portant le même nom). || — zusätzliche Header, die mit dieser Anfrage gesendet werden (sie überschreiben gleichnamige Header der Verbindung). || — header aggiuntivi inviati con questa richiesta (sostituiscono quelli della connessione con lo stesso nome). || — このリクエストと一緒に送る追加ヘッダー（接続の同名ヘッダーより優先されます）。 || — 随此请求一起发送的额外请求头（会覆盖连接中的同名请求头）。
— sent as ?name=value, e.g. a filter like situacao = A or limit = 50. || — enviados como ?nome=valor, por exemplo um filtro como situacao = A ou limit = 50. || — se envían como ?nombre=valor, por ejemplo un filtro como situacao = A o limit = 50. || — envoyés sous la forme ?nom=valeur, par exemple un filtre comme situacao = A ou limit = 50. || — werden als ?name=wert gesendet, z. B. ein Filter wie situacao = A oder limit = 50. || — inviati come ?nome=valore, ad esempio un filtro come situacao = A o limit = 50. || — ?name=value の形で送信されます。例: situacao = A や limit = 50 のようなフィルター。 || — 以 ?name=value 的形式发送，例如 situacao = A 或 limit = 50 这样的筛选条件。
{1} header(s) || {1} cabeçalho(s) || {1} cabecera(s) || {1} en-tête(s) || {1} Header || {1} header || {1} 件のヘッダー || {1} 个请求头
?{1} param(s) || ?{1} parâmetro(s) || ?{1} parámetro(s) || ?{1} paramètre(s) || ?{1} Parameter || ?{1} parametro/i || ?{1} 件のパラメーター || ?{1} 个参数
