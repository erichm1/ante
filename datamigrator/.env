# Copy to .env and fill in.
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True

# Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# This key encrypts every stored credential (OAuth2 tokens, API keys, basic auth
# passwords) at rest. Losing it makes existing secrets unrecoverable — back it up
# outside of source control.
FIELD_ENCRYPTION_KEY=gtk3h1mDg1GuCJNoaUFTDwxfngS3qmbBg9TJ4LxdGLU=
